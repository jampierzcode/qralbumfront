# Vacío = mismo origen (en desarrollo Vite reenvía /api y /media al backend)
VITE_API_URL=
# Backend al que reenvía el servidor de desarrollo
VITE_API_PROXY=http://localhost:3001
# Dominio de los links/QR que se comparten (ej. https://regalos.midominio.com o http://192.168.1.20:3000)
VITE_PUBLIC_URL=
